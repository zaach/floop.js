DEFINE PROCEDURE ''REMAINDER'' [M,N]:
BLOCK 0: BEGIN
	IF N = 0, THEN:
	QUIT BLOCK 0;
	CELL(0) ⇐ 0;
	LOOP AT MOST M TIMES:
	BLOCK 1: BEGIN
		CELL(1) ⇐ CELL(0) + N;
		IF CELL(1) > M, THEN:
		ABORT LOOP 1;
		CELL(0) ⇐ CELL(1);
	BLOCK 1: END;
	OUTPUT ⇐ MINUS [M,CELL(0)];
BLOCK 0: END.
